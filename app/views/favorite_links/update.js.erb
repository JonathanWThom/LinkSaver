rawFavorite = "<%= j render 'links/favorites', link: @link %>";
wrapper = document.createElement("div");
wrapper.innerHTML = rawFavorite;
favorite = wrapper.firstChild;
document.querySelector(".favorite[data-link-id='<%= @link.id %>']").replaceWith(favorite);
