h3 settings
hr

= link_to "home", links_path
hr

p
  = render "users/display_name"
hr

p
  = render "links/delete_old_links", paginate: true

hr

p
  = link_to "export", link_exports_path(format: :csv)

= render "color_modes/show"

p
  = link_to "donate", "https://commerce.coinbase.com/checkout/0367ed6d-9798-482a-a541-a86cabe53b5a"

p
  = link_to "delete account", user_path(current_user), method: :delete, data: { confirm: "deleting your account is irreversible."}
