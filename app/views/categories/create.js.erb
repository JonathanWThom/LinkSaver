rawTagForm = "<%= j render 'tags/add_form', link: @link %>";
wrapper = document.createElement("div");
wrapper.innerHTML = rawTagForm;
document.getElementById("tag_link_<%= @link.id %>").replaceWith(wrapper.firstChild);
